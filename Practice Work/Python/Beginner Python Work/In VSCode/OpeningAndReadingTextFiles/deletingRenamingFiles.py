'''
Two other useful functions are remove() and rename() functions

These are avaiable in the os module and need to be imported

remove() -- function deletes a file.  The syntax is remove(filename)
e.x: remove('myfile.txt)

rename() -- renames a file.  The syntax is (old name, new name)
e.x: rename('oldfile.txt', 'newfile.txt')

'''